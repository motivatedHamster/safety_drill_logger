<%= datagrid_form_for @grid, :method => :get, :url => search_path %>

<%= paginate(@grid.assets) %>
<%= datagrid_table @grid %>
<%= paginate(@grid.assets) %>

<br />
<%= link_to new_drill_path, class: 'btn btn-primary' do %>
  <span class="glyphicon glyphicon-plus"></span>
  New Drill
<% end %>
